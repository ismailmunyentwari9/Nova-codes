<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>View Books</title>
</head>
<body>
    <main class="w-[80%] mx-auto border border-gray-500 h-[auto] mt-5">
     <h1 class="text-center"> WEllcome To Book Shop</h1>

     <div class="book-container grid grid-cols-2 gap-3 p-2">
        <%data.forEach(item=>{ %>
            <div class="border border-gray-900 p-2 flex flex-col items-center">
                <div class="flex flex-col justify-between">
                    <p><span class="text-red-300">Book Name:</span><%=item.book_name%></p>
                <p><span class="text-red-300">Book Title:</span><%=item.book_title%></p>
                <p><span class="text-red-300">Book Author:</span><%=item.book_author%></p>
            </div>
                <div class="flex gap-[4rem] mt-5">
                
                    <form action="/edit/<%=item.book_id%>" method="POST"><button>EDIT 🖊</button></form>
                   <form action="/delete/<%=item.book_id%>" method="POST"> <button>DELETE🚮</button></form>
                </div>
            </div>
       <% })%>
     </div>
    </main>
</body>
</html>